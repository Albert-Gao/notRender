import React from 'react';
import GoBackButton from '../../components/GoBackButton';

const LoginScreen = () => (
  <div>
    <GoBackButton />
    Login Screen
  </div>
);

export default LoginScreen;
